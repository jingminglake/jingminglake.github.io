# 算法学习进度

> 本文件由 leetcode agent 维护，记录学习状态、复习队列和蓝图卡片。
> 每次学习会话开始时先读本文件，结束时更新。

## 当前状态

- **当前主题**：排序双指针与 kSum 家族 — 对应博文《Leetcode总结（二十二）》
- **进行中的题目**：无（923 已讲完，下一题候选见学习路线）
- **已完成的题目**：167、15（补充去重细节）、16、18、259（回顾）、611、923
- **上一主题**：滑动窗口（深化）— 对应博文《Leetcode总结（二十一）》，断点停在 438（找所有字母异位词），已完成 1004、3、76

## 复习队列（间隔重复：3天 → 1周 → 2周 → 1月）

| 内容 | 学习日期 | 下次复习 | 复习方式 | 轮次 |
|------|---------|---------|---------|------|
| 可变窗口蓝图（三问一前提） | 2026-06-11 | 2026-07-01 | 不看笔记复述 | 第1轮（表达需加强） |
| Leetcode 1004 | 2026-06-09 | 2026-07-01 | 白板重写代码 | 第1轮（表达需加强） |
| Leetcode 3 | 2026-06-11 | 2026-07-01 | 白板重写代码 | 第1轮（表达需加强） |
| Leetcode 76 | 2026-06-24 | 2026-06-27 | 白板重写代码 + 复述蓝图 | 第1轮（已逾期） |
| 求最大 vs 求最小蓝图对比 | 2026-06-24 | 2026-06-27 | 不看笔记复述 | 第1轮（已逾期） |
| kSum 蓝图三问 | 2026-07-25 | 2026-07-28 | 不看笔记复述 | 第1轮 |
| Leetcode 15 三处去重 | 2026-07-25 | 2026-07-28 | 白板重写代码 | 第1轮（本次讲解中未想起，需重点复习） |
| Leetcode 923 成段计数 | 2026-07-25 | 2026-07-28 | 白板重写代码 | 第1轮 |
| 611 为何外层固定最大边 | 2026-07-25 | 2026-07-28 | 不看笔记复述 | 第1轮 |

复习规则：复述/重写成功 → 进入下一轮（间隔翻倍）；失败 → 重新讲解，间隔重置为3天。

## 蓝图卡片

### 卡片：可变滑动窗口（核心蓝图）

**根本前提**：单调性 — 扩大窗口合法性只变差，缩小窗口合法性只变好。新题第一步先验证这个，不满足就不能用。

**三个核心问题**：
1. **窗口内维护什么？** 由"合法"的定义决定（计数器 / hash map / 单调队列）
2. **right 进攻** — 走到位置 i 时，left 之前所有位置与 i 的组合已全部处理，不遗漏
3. **left 防守** — 触发时机和目标由求最大还是求最小决定（见下方对比）

**求最大 vs 求最小 — left 的触发时机正好相反**：

| | 求最大窗口（如 1004） | 求最小窗口（如 LC 76） |
|--|---------------------|----------------------|
| left 移动时机 | 窗口**不合法**时 | 窗口**合法**时 |
| left 停止条件 | 停到刚好合法 | 停到刚好不合法 |
| left 的目标 | 找第一个合法位置 | 找最后一个合法位置（沿途记录答案） |

**复杂度**：摊还分析 — 单次 while 可能多步，但 left/right 全程只增不减，总计 O(2N)。

### 卡片：1004 — 至多 k 个 0

- 维护：`zeros`（窗口内 0 的个数）
- 不合法：`zeros > k`
- 收缩：while，每次 right 最多引入 1 个 0，但消除它可能要扫过多个 1

### 卡片：Leetcode 3 — 无重复字符

- 维护：hash map（字符 → 最后出现下标）
- 不合法：新字符的 `last_seen[c] >= left`（在窗口内才算重复）
- 收缩：**不需要 while**，`left = last_seen[c] + 1` 一跳到位
- 为什么一跳到位：每次 right 只引入一个字符，窗口内最多一个旧副本

### 卡片对比：while vs 一跳到位

| | 1004 | LC 3 |
|--|------|------|
| 每次 right 引入几个违规 | 最多 1 个 0 | 最多 1 个重复 |
| 消除违规需要几步 | 不定（要扫过 1）→ while | 恰好 1 步（位置已知）→ if |

### 卡片：kSum 家族（核心蓝图）

**根本前提**：排序。有序后 left 右移变大、right 左移变小，每步能排除一整行/列，而非一个格子。

**三个核心问题**：
1. **数组有序吗？** 无序且只求两数 → 哈希（O(N) 空间）；否则先排序用双指针（O(1) 空间）
2. **要内容还是要数量？** 要内容 → 去重；要数量 → 不去重，但要想清一次加多少个
3. **外层固定哪一个？** 多数题固定最小（内层从 `i+1` 起）；由"最大边"定义合法性的题（611）固定最大

**降维**：kSum = 前 k-2 层循环固定 + 最后 2 层双指针，O(N^(k-1))。内层从 `i+1` 开始是为了避免重复使用同一下标。

### 卡片：15 的三处去重

1. 外层：`i > 0 && nums[i] == nums[i-1]` → continue
2. 内层：找到解后 left 右跳、right 左跳，越过所有相同值
3. **跳完各再走一步并继续扫描** ← 本次讲解中未想起，是最易漏的一处

为什么要继续扫：同一个 i 下内层可能有多个不同 pair 都成立（如 i=-3，`[-1,1,2,4]` 中 `-1+4` 和 `1+2` 都是 3）。

### 卡片对比：去重 vs 计数

| | 15（要内容） | 923（要数量） |
|--|-------------|--------------|
| 遇到一段重复值 | 跳过 | 用组合数数清楚 |
| 原因 | 值相同的三元组算重复 | 下标不同就算不同答案 |
| 操作 | while 跳过 + 各走一步 | `c1*c2` 或 `C(m,2)` |

### 卡片对比：16 vs 259

扫描过程完全一样（left/right 交替往中间收，O(N) 步）。区别只在每步做什么：
- 16：更新一个最优值
- 259：`res += right - left` 累加一整行，注意这只消掉一行不是整张表，仍要继续走

## 学习路线（kSum 篇）

- [x] 1. Two Sum（旧笔记：总结十二）
- [x] 167. Two Sum II
- [x] 1099. Two Sum Less Than K（旧笔记：总结二）
- [x] 15. 3Sum（旧笔记：总结二，本次补充去重细节）
- [x] 16. 3Sum Closest
- [x] 18. 4Sum
- [x] 259. 3Sum Smaller（旧笔记：总结二）
- [x] 611. Valid Triangle Number
- [x] 923. 3Sum With Multiplicity
- [ ] 11. Container With Most Water（双指针的贪心论证，与 kSum 不同）
- [ ] 42. Trapping Rain Water（旧笔记：总结二，可回顾深化）
- [ ] 713. Subarray Product Less Than K（计数思路与 259 呼应）

## 学习路线（滑动窗口篇）

- [x] 1004. Max Consecutive Ones III
- [x] 3. Longest Substring Without Repeating Characters
- [x] 76. Minimum Window Substring
- [ ] 438. Find All Anagrams in a String
- [ ] 424. Longest Repeating Character Replacement
- [ ] 239. Sliding Window Maximum（单调队列）
- [ ] 904. Fruit Into Baskets
- [ ] 992. Subarrays with K Different Integers（恰好k = 至多k - 至多k-1）

下一主题候选：单调栈、拓扑排序
